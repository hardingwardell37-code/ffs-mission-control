export function AgentForm({ action, agent }: { action: (data: FormData) => Promise<void>; agent?: Record<string, string | number> }) { return <form action={action} className="form panel">{agent?.id && <input type="hidden" name="id" value={agent.id}/>}<div className="form-grid"><label>Name<input name="name" required maxLength={120} defaultValue={agent?.name}/></label><label>Slug<input name="slug" required pattern="[a-z0-9]+(?:-[a-z0-9]+)*" defaultValue={agent?.slug}/></label></div><label>Purpose<input name="purpose" required maxLength={500} defaultValue={agent?.purpose}/></label><label>Description<textarea name="description" maxLength={1000} defaultValue={agent?.description}/></label><label>System instructions<textarea name="instructions" required rows={8} defaultValue={agent?.system_instructions}/></label><div className="form-grid"><label>Provider<input name="provider" required defaultValue={agent?.model_provider ?? "openai"}/></label><label>Model<input name="model" required defaultValue={agent?.model_name}/></label><label>Runtime limit (seconds)<input name="maxRuntimeSeconds" type="number" min="1" max="3600" required defaultValue={agent?.max_runtime_seconds ?? 300}/></label><label>Status<select name="status" defaultValue={agent?.status ?? "draft"}><option>draft</option><option>active</option><option>paused</option><option>disabled</option></select></label></div><button className="button" type="submit">Save governed agent</button></form> }
