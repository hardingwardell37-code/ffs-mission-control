export function AgentForm({ action, agent }: { action: (data: FormData) => Promise<void>; agent?: Record<string, string | number | null> }) { return <form action={action} className="form panel">{agent?.id && <input type="hidden" name="id" value={String(agent.id)}/>}<div className="form-grid"><label>Name<input name="name" required maxLength={120} defaultValue={String(agent?.name ?? "")}/></label><label>Slug<input name="slug" required pattern="[a-z0-9]+(?:-[a-z0-9]+)*" defaultValue={String(agent?.slug ?? "")}/></label><label>Department<input name="department" maxLength={80} defaultValue={String(agent?.department ?? "")} placeholder="Marketing"/></label></div><label>Purpose<input name="purpose" required maxLength={500} defaultValue={String(agent?.purpose ?? "")}/></label><label>Description<textarea name="description" maxLength={1000} defaultValue={String(agent?.description ?? "")}/></label><label>System instructions<textarea name="instructions" required rows={8} defaultValue={String(agent?.system_instructions ?? "")}/></label><div className="form-grid"><label>Provider<input name="provider" required defaultValue={String(agent?.model_provider ?? "openai")}/></label><label>Model<input name="model" required defaultValue={String(agent?.model_name ?? "")}/></label><label>Runtime limit (seconds)<input name="maxRuntimeSeconds" type="number" min="1" max="3600" required defaultValue={Number(agent?.max_runtime_seconds ?? 300)}/></label><label>Status<select name="status" defaultValue={String(agent?.status ?? "draft")}><option>draft</option><option>active</option><option>paused</option><option>disabled</option></select></label></div><button className="button" type="submit">Save governed agent</button></form> }
