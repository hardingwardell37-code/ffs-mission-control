import { login } from "./actions";
import Link from "next/link";
export default async function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string; reset?: string }> }) { const { error, reset } = await searchParams; return <main className="auth-shell"><div className="auth-card"><div className="eyebrow">Restricted system</div><h1>Operator sign in.</h1><p className="lede">Authenticate to enter the Forged Field Systems control plane.</p>{error && <p className="error">Authentication failed. Check your credentials and configuration.</p>}{reset && <p className="notice">Password updated. Sign in with your new password.</p>}<form action={login} className="form"><label>Email<input name="email" type="email" required autoComplete="email" /></label><label>Password<input name="password" type="password" required autoComplete="current-password" /></label><button className="button" type="submit">Enter Mission Control</button><Link className="auth-link" href="/forgot-password">Forgot password?</Link></form></div></main>; }
